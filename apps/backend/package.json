{
  "name": "backend",
  "private": true,
  "scripts": {
    "dev": "convex dev",
    "typecheck": "tsc --noEmit -p convex/tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "convex": "^1.29.1"
  },
  "devDependencies": {
    "@edge-runtime/vm": "^5.0.0",
    "@types/node": "^20",
    "convex-test": "^0.0.41",
    "typescript": "^5",
    "vitest": "^4.0.17"
  }
}
